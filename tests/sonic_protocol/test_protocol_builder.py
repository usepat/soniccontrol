
# correct commands with version get selected. min_version, deprecated_version
# correct commands for device get selected. include, exclude devices
# correct answers are also selected. Only one and not multiple
# only commands with is_release are selected if mode = release 
# error gets thrown if version higher then that of the protocol
 